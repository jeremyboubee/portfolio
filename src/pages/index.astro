---
import Home from "../components/Home.astro";
import PortfolioLayout from "../layouts/PortfolioLayout.astro";
import type { Props as Config } from "../layouts/PortfolioLayout.astro";

const config: Config = {
  language: "fr",
  title: "wip",
};
---

<PortfolioLayout language={config.language} title={config.title}>
  <Home />
</PortfolioLayout>
